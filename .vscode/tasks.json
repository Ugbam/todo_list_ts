{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tsc: build - tsconfig.json", // Must match preLaunchTask in launch.json
            "type": "typescript",
            "tsconfig": "tsconfig.json",
            "problemMatcher": [
                "$tsc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}